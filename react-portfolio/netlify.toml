[build]
  command = "npm run build"
  publish = "dist"
  base = "/"

[build.environment]
  NODE_VERSION = "18"  # Changed from 20 to more stable version

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
